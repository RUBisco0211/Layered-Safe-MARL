# BUG_REPORT — Layered-Safe-MARL 训练/评估问题汇总

> 生成时间：2026-08-21
> 背景：为让「阶段2 带安全过滤训练 + eval 渲染」跑通，排查/验证过程中发现的待办问题与已完成的修复。
> 机器：my.wansen.co（conda env: layer-safe-marl，torch 2.2.1+cu121，numpy 1.26.4）

---

## 0. 已完成的修复（上下文，勿重复修）

1. **hj_reachability 库 bug（阶段2 崩溃的元凶）**
   - 位置：`site-packages/hj_reachability/dynamics.py`
   - 问题：`Dynamics.optimal_control()` / `optimal_disturbance()` 调用 `self.optimal_control_and_disturbance(self, state, time, grad_value)` 时**多传了一个 `self`** → `TypeError: ... takes 4 positional arguments but 5 were given`。
   - 修复：已提供 `patches/apply_hj_reachability_patch.sh`（重装 `hj-reachability==0.5.0` 并自动打补丁）。⚠️ 这是 site-packages 里的补丁，**重建环境/重装 hj-reachability 后需重新执行该脚本**。
2. **hj_reachability_utils 纳入主仓库 git 管理**
   - 之前是嵌套独立 repo（自带 `.git`，且被 `.gitignore` 的 `hj_reachability_utils/` 忽略），改动在 `git status` 里不可见。
   - 已删除嵌套 `.git`、去掉 `.gitignore` 规则、`git add` 纳入主仓库；包含补的 `__init__.py`（修复 import）。
3. **环境依赖**：`opencv-python==4.10.0.84`（cv2）；numpy 必须保持 `1.26.4`（不要升 2.x，否则 wandb 0.15.4 的 `np.float_` 崩溃）。

---

## 1. 【待修】eval 保存 mp4 失败：OpenCV 缺 avc1(H.264) 编码器

- **现象**：`scripts/eval_mpe.py` 渲染跑完后，`graph_mpe_runner.py` render() 里 `cv2.VideoWriter(fourcc=*avc1)` 报 `Could not find encoder for codec_id=27 / Failed to initialize VideoWriter`；代码仍打印 `Video saved to ...`，但**实际没有生成 .mp4**。\n- **影响**：eval 的 CSV 日志（position/safety/min_distance/eval_log）正常生成，但视频缺失。\n- **根因**：当前 OpenCV 构建的 FFmpeg 没有 H.264(avc1) 编码器。\n- **可能修复**：\n  1. 最简单：`graph_mpe_runner.py` 中 `cv2.VideoWriter_fourcc(*avc1)` 改为 `cv2.VideoWriter_fourcc(*mp4v)`（MPEG-4，通常自带）或 `*XVID` + `.avi`。\n  2. 安装带 H.264 的 ffmpeg 供 OpenCV 使用（需 sudo/apt，当前服务器无 sudo）。\n  3. 改用项目已依赖的 imageio 写 mp4，绕过 OpenCV 编码器。\n\n## 2. 【待修】eval_airtaxi.sh 与模型配置不匹配 + bayarea 底图缺失\n\n- **现象1**：`eval_airtaxi.sh` 用 `--model_dir=trained_models/airtaxi_safety_informed --num_agents=8 --scenario_name=navigation_graph_safe_bayarea_merge`，但该模型 `config.yaml` 实际是 `scenario_name: navigation_graph_safe`、`num_agents: 4`、`world_size: 6`、`num_landmarks: 2`。\n- **现象2**：bayarea 场景 `navigation_graph_safe_bayarea_merge/cross` 需要 `multiagent/custom_scenarios/data/bayarea_merge.jpg` / `bayarea_cross.jpg` 底图，但 `multiagent/custom_scenarios/data/` 目录不存在；且 `.gitignore` 的 `data/` 规则会忽略它。\n- **影响**：直接跑 `eval_airtaxi.sh` 会 `FileNotFoundError: Image file ... bayarea_merge.jpg not found`。\n- **可能修复**：\n  1. 统一 `eval_airtaxi.sh` 参数与模型训练配置一致（`navigation_graph_safe` + `num_agents=4` + `world_size=6` + `num_landmarks=2`）。\n  2. 或提供 bayarea 底图并放入 `multiagent/custom_scenarios/data/`（注意处理 `.gitignore`）。\n  3. 或让 eval 脚本按 model_dir 的 config.yaml 自动对齐场景/agent 数（`eval_mpe.py` 已有 `modify_args`，可把 `num_agents/scenario/world_size` 也从 config 读，而不是被 CLI 覆盖）。\n\n## 3. 【待修】eval 无头渲染需设置 SAFE_MARL_HEADLESS_RENDER=true\n\n- **现象**：服务器无 DISPLAY，直接跑 eval 渲染会 `pyglet.canvas.xlib.NoSuchDisplayException: Cannot connect to "None"`。\n- **现状**：`multiagent/rendering.py` 已支持环境变量 `SAFE_MARL_HEADLESS_RENDER=true`（设后 pyglet 走 headless + 不可见窗口，已验证可用）。\n- **影响**：`eval_airtaxi.sh` / `eval_double_integrator.sh` 没设该变量，在无显示服务器上会崩。\n- **可能修复**：在 eval 脚本开头加 `export SAFE_MARL_HEADLESS_RENDER=true`，或做成可开关/默认开启。\n\n## 4. 【待查】filter 训练冒烟时出现 `NaN or Inf found in input tensor`\n\n- **现象**：`scripts/train_mpe.py --use_safety_filter True`（从 warmstart 模型续跑）在第 1 个 episode 后打印两次 `NaN or Inf found in input tensor`（torch 前向/反向输入含 NaN）。训练未崩，指标正常（done=100%、conflict=7.3%）。\n- **可能原因**（需排查）：\n  1. 安全过滤在边界处产生非法动作/状态，或 HJ 插值在网格外返回 NaN。\n  2. PopArt/value_norm 归一化在初始阶段遇到极端值。\n  3. 奖励/距离计算里对 0 取 log 等。\n- **建议**：跑更多 episode 观察是否持续；在 `apply_safety_filter` 与 reward 计算处打印/检查 NaN 来源。\n\n## 5. 【注意】GPU 被其他用户占用\n\n- 服务器上有其他用户（`hzh`）的 Ray 训练占用 GPU 0/1/2（util 95-100%）。\n- 跑训练/评估前用 `nvidia-smi` 确认 GPU 空闲，必要时换卡。\n\n## 6. 【注意】训练进程管理（踩过的坑）\n\n- 用 `job-start`（foreground-mirrored）跑训练时，**不要在训练运行期间对该会话执行 `exec`**，否则可能给前台进程发 SIGINT 导致 `KeyboardInterrupt`。\n- 若 job 跟踪丢失（`job-poll` 报 not found），可用 sftp 查 wandb `output.log` 看进度。\n- 进程定位：训练主进程会被 `setproctitle` 改名成 `rmappo-GraphMPE-...`，用 `pgrep -af rmappo` 才能找到（`pgrep -f train_mpe.py` 匹配不到）。\n\n---\n\n## 验证命令速查\n\n- **eval 最小跑通**（navigation_graph_safe，无需底图）：\n  ```\n  cd /home/linminlei/code/Layered-Safe-MARL\n  SAFE_MARL_HEADLESS_RENDER=true python scripts/eval_mpe.py \\\n    --model_dir=trained_models/airtaxi_safety_informed --dynamics_type airtaxi \\\n    --render_episodes=1 --num_agents=4 --num_obstacles=0 --num_landmarks=2 --seed=0 \\\n    --episode_length=50 --use_dones=False --collaborative=False \\\n    --scenario_name=navigation_graph_safe --horizon=1 --save_gifs --use_render \\\n    --num_walls=0 --discrete_action=True --use_masking True --use_safety_filter True --world_size=6\n  ```\n- **阶段2 filter 冒烟**（需先跑过阶段1 warmstart，`--model_dir` 指到含 actor.pt 的 `.../files` 目录）：\n  ```\n  CUDA_VISIBLE_DEVICES=<gpu> python -u scripts/train_mpe.py \\\n    --use_wandb --use_valuenorm --use_popart --project_name smoke_test \\\n    --env_name GraphMPE --algorithm_name rmappo --seed 0 --experiment_name smoke_filter_test \\\n    --scenario_name navigation_graph_safe --dynamics_type airtaxi \\\n    --num_agents=4 --num_landmarks=2 --n_training_threads 1 --n_rollout_threads 32 \\\n    --num_mini_batch 1 --episode_length 350 --num_env_steps 22400 \\\n    --ppo_epoch 10 --use_ReLU --gain 0.01 --lr 7e-4 --critic_lr 7e-4 \\\n    --user_name 1205456072-southeast-university \\\n    --use_cent_obs False --graph_feat_type relative --use_dones False --collaborative False \\\n    --num_walls 0 --world_size=6 --auto_mini_batch_size --target_mini_batch_size 4096 \\\n    --use_safety_filter True --num_internal_step 1 --use_masking True --checkpoint_interval 50 \\\n    --model_dir=<warmstart model dir>/files\n  ```\n